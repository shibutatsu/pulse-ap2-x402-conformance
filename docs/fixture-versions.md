# Fixture versions preserve source provenance as well as expected decisions

| Fixture | Status | AP2 source | x402 source | Raw file SHA-256 |
| --- | --- | --- | --- | --- |
| `fixtures/v0.1/cases.json` | Archived and read-only | `e1ea56db72a6385bce3e5c1112b3a56ce60acb43` | `67b1ba0a7abbd7907a28fa624670872532e0eae9`, packages 2.19.0 | `4ff061cfa709b043662e67335bf4abd0e8dcf8cb45d32ee333992e3789d95a80` |
| `fixtures/v0.2/cases.json` | Current and regenerated by CI | `e1ea56db72a6385bce3e5c1112b3a56ce60acb43` | `c8247c4cd15f29498474404d94636e7dbb894e86`, packages 2.22.0 | `326de97fde74636bf4c2b8c6838548cb5f091f754189af2c1a9d25aae92c1ec0` |

Both bundles contain the same 20 accepted and 60 fail-closed decisions. The v0.2 case and bundle
versions changed because every case-level `inputHash` covers its source pins. Replacing the x402
package version without changing the fixture version would make the recorded provenance ambiguous,
even if the protocol payload bytes happened to remain compatible.

The verifier accepts either complete bundle. It rejects a case copied across versions, and the
external-reproduction checker rejects a record whose fixture path does not match the supplied
bundle version. Fixture generation writes v0.2 only and checks the x402 versions in `package.json`
before producing any output. The v0.1 corpus stays byte-for-byte identical to the corpus generated
under x402 2.19.0.
