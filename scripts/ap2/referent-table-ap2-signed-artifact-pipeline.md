| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 候補語 | 初出定義 |
| --- | --- | --- | --- | --- | --- | --- |
| AP2 Python SDK `mandate.py` / 公開fixture要件 | 20件の合成ケースごとに発行者署名と開示を再現する | merchant issuer が署名した Open Payment Mandate の SD-JWT と、その disclosure 群 | 記録 | 固定鍵を読み込んだ後、terminal closed mandate を作る前 | Open Payment Mandate artifact | Open Payment Mandate artifact とは、merchant issuer が署名した Open Payment Mandate の SD-JWT と disclosure 群を一つの fixture 記録にしたものを指す。 |
| AP2 Python SDK `mandate.py` / 公開fixture要件 | terminal が merchant 署名を検証して支払手段を選択した事実を表す | Open Payment Mandate を親に持ち、holder key binding を含む terminal closed Payment Mandate の KB-SD-JWT | 記録 | Open Payment Mandate の発行後、Payment Receipt の発行前 | Terminal Closed Payment Mandate artifact | Terminal Closed Payment Mandate artifact とは、Open Payment Mandate を親に持ち、terminal の holder key binding を含む closed Payment Mandate の KB-SD-JWT を指す。 |
| AP2 Python SDK `get_closed_mandate_jwt` / 公開fixture要件 | x402 nonce と Payment Receipt reference の共通参照値を固定する | terminal closed Payment Mandate presentation の leaf issuer JWT を SHA-256 した32 byte値 | 値 | terminal closed mandate の生成後、receipt と normalized record の生成前 | closed mandate reference | closed mandate reference とは、terminal closed Payment Mandate presentation から SDK が取り出す leaf issuer JWT の SHA-256 値を base64url 表現したものを指す。 |
| AP2 Python SDK `PaymentInstrument` / Issue #299 / 公開fixture要件 | x402 支払条件を AP2 署名対象から落とさず検証へ渡す | `id` と `type` に加えて x402 scheme/network/asset/amount/payTo/payer/timeout/domain/nonce binding を保持する payment instrument | 記録 | Open Payment Mandate の発行前に埋め込み、closed mandate の検証後に raw claims から抽出 | x402-preserving PaymentInstrument | x402-preserving PaymentInstrument とは、AP2 の `id` と `type` に x402 支払条件を追加し、Pydantic の検証後も追加フィールドを保持する fixture 専用モデルを指す。 |
| AP2 Python SDK `PaymentReceipt` / 公開fixture要件 | 支払結果を closed mandate 参照へ結び付ける | merchant issuer が署名し、`payment.reference` に closed mandate reference を持つ Payment Receipt JWT | 記録 | closed mandate reference の確定後、normalized record の出力前 | Payment Receipt artifact | Payment Receipt artifact とは、merchant issuer が署名し、`payment.reference` が closed mandate reference と一致する Payment Receipt JWT を fixture に記録したものを指す。 |
| 公開fixtureの決定性要件 | Node 側の適合性検証へ SDK 検証済み入力を決定的に渡す | 署名・key binding・audience/nonce・時刻・制約・receipt reference の検証結果と raw claims を整形した JSON | 記録 | 3つの署名アーティファクトを再検証した後、fixture ファイルをコミットする前 | normalized AP2 verification record | normalized AP2 verification record とは、AP2 SDK による署名・key binding・audience/nonce・時刻・制約・receipt reference の再検証結果と raw claims を決定的な JSON 形へ整えた記録を指す。 |
