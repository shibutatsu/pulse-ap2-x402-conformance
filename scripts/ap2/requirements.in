# Direct runtime and test dependencies declared by the pinned AP2 Python SDK.
cryptography==46.0.5
jwcrypto==1.5.7
pydantic==2.12.5
sd-jwt==0.10.4
pytest==9.0.2
py==1.11.0

# Pinned PEP 517 backend used to build the exact clean AP2 checkout without
# downloading an unpinned build dependency into an isolated environment.
setuptools==80.9.0
